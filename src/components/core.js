import {
    View, StyleSheet, Text, TextInput, Image, TouchableHighlight, AsyncStorage, NetInfo, 
    ScrollView, TouchableWithoutFeedback, Picker
} from 'react-native'

export {
    View, Text, TextInput, Image, StyleSheet,
    TouchableHighlight,
    AsyncStorage,NetInfo,
    ScrollView,
    TouchableWithoutFeedback,
    Picker
}